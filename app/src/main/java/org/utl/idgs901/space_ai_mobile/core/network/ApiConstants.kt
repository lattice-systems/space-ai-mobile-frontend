package org.utl.idgs901.space_ai_mobile.core.network

object ApiConstants {
    // In a real project, this would come from BuildConfig
    const val BASE_URL = "http://10.0.2.2:5000/api/" // Default local emulator URL for ASP.NET
    const val TIMEOUT_SECONDS = 30L
}
